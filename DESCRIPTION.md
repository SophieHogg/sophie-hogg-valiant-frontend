# TODO:

* Get it running on local machine ✅
* Flesh out folder structure (services, components, tests) ✅
* Create link to the backend to fetch relevant data ✅
* Create simple form ✅
* Ensure outputs are returning as necessary (testing) ✅
* Add styling and design ✅
* Add special details (tooltips, etc.) ✅
* Throughout: Testing

Testing: 
- Tested the PMT function inputs and outputs with Google Sheets and added these tests to PMT, ensuring every variable was accounted for
- Added unit tests to App.vue to ensure the calculation logic works
- Added unit tests to the RepaymentAmount.vue to ensure the output renders correctly
- Added Cypress tests to ensure that the UI is usable and also calculates correctly with tooltips