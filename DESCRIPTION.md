TODO:

##

1. Get it running on local machine
2. Flesh out folder structure (services, stores, components)
    * tests will sit alongside the relevant section, not split out into their own mirrored section - easier to find them
3. Create link to the backend to fetch relevant data
4. Create simple form
    * Step 3 and 4 are interchangeable, so this may change later
5. Ensure outputs are returning as necessary (testing)
6. Add styling and design
7. Add special details (tooltips, etc.)


Throughout: test (writing tests at the end is laborious and it's easier to write tests with full context of the component. TBD whether to write tests)

